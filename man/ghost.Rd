% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ghost.R
\name{ghost}
\alias{ghost}
\title{Redact transcripts using a local app}
\usage{
ghost(
  input_dir = NULL,
  output_dir = NULL,
  interviewers = character(),
  interviewees = character(),
  redact_other = character(),
  redact_interviewer = TRUE,
  include_common_names = FALSE,
  redacted_token = "[REDACTED]",
  add_blank_line_between_turns = TRUE,
  recursive = FALSE,
  suffix = "_redacted",
  out_format = NULL,
  report_redacted = FALSE,
  name_review_min_score = 2,
  show_completion_notice = TRUE
)
}
\arguments{
\item{input_dir}{Optional folder containing transcripts. If \code{NULL}, the app
asks for it.}

\item{output_dir}{Optional folder to write outputs. If \code{NULL}, the app asks
for it, defaulting to \code{input_dir} when left blank.}

\item{interviewers}{Optional character vector of interviewer names to prefill
in the app.}

\item{interviewees}{Optional character vector of interviewee/participant names
to prefill in the app.}

\item{redact_other}{Optional terms to prefill in the app's other-redaction
field.}

\item{redact_interviewer}{Default value for the app option that controls
whether interviewer names are redacted in body text.}

\item{include_common_names}{Default value for the app option that includes
the package's default common-name list when available.}

\item{redacted_token}{Default replacement token shown in the app.}

\item{add_blank_line_between_turns}{Default value for the app option that
inserts a blank line between turns for DOCX/TXT outputs.}

\item{recursive}{Default value for the app option to include subdirectories.}

\item{suffix}{Default suffix shown in the app for output filenames.}

\item{out_format}{Default output format shown in the app. One of \code{"vtt"},
\code{"docx"}, or \code{"txt"}. If \code{NULL}, the app defaults to keeping original
formats.}

\item{report_redacted}{Default value for the app option that prints
redaction summaries to the R console.}

\item{name_review_min_score}{Minimum rule-based score for a candidate to
appear in the review app.}

\item{show_completion_notice}{Default value for the app option that opens a
local completion notice after all outputs are written.}
}
\value{
A data.frame with \code{input_file}, \code{output_file}, \code{status}, and redaction
report columns. Invisibly returned.
}
\description{
Opens a local Shiny app that asks for an input directory, output directory,
known interviewer names, known participant names, and other terms to redact.
The app scans supported transcript files in the input directory, shows likely
names for review, and writes redacted transcript files. Supported input
types are \code{.docx}, \code{.txt}, and \code{.vtt}.
}
\examples{
# In an interactive R session:
# ghost()
}
