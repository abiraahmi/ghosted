% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ghost_docx.R
\name{ghost_docx}
\alias{ghost_docx}
\title{Read a Word file and redact it (no data.frame)}
\usage{
ghost_docx(
  filepath,
  interviewers,
  interviewees = character(),
  redact_other = character(),
  redact_interviewer = FALSE,
  include_common_names = FALSE,
  redacted_token = "[REDACTED]",
  add_blank_line_between_turns = TRUE,
  output_path = NULL,
  suffix = "_redacted",
  out_format = c("docx", "txt", "vtt"),
  report_redacted = FALSE
)
}
\arguments{
\item{filepath}{Path to a \code{.docx} file.}

\item{interviewers}{Character vector of interviewer names.}

\item{interviewees}{Character vector of interviewee/participant names.}

\item{redact_other}{Other words/phrases to redact.}

\item{redact_interviewer}{If \code{TRUE}, also redact interviewer names.}

\item{include_common_names}{If \code{TRUE}, also redact a default list of common
names (e.g., top US baby names, if available via
\code{ghosted::common_names_default}).}

\item{redacted_token}{Replacement token used for redactions (names and other
phrases).}

\item{add_blank_line_between_turns}{Logical; for TXT/DOCX outputs when
converting formats, insert a blank line between turns. This does not affect
DOCX→DOCX.}

\item{output_path}{Path for the redacted file. If \code{NULL} (default), set to the
same directory and base name as \code{filepath} with \verb{_redacted} before the
extension. The extension is chosen based on \code{out_format} (e.g.
\code{report.docx} -> \code{report_redacted.docx} for \code{out_format = "docx"}, or
\code{report_redacted.txt} / \code{report_redacted.vtt} otherwise).}

\item{suffix}{Suffix to append to the base filename (default: \code{"_redacted"}).
Only used when \code{output_path} is \code{NULL}.}

\item{out_format}{One of \code{"docx"}, \code{"txt"}, or \code{"vtt"} controlling the output
file type. Defaults to \code{"docx"}.}

\item{report_redacted}{If \code{TRUE}, print to the R console which phrases were
found and redacted (names and other).}
}
\value{
The path to the written output file (invisibly).
}
\description{
Reads a \code{.docx} file as raw paragraphs, applies in-function redaction, and writes a
redacted file. No speaker/text data.frame is created; the document is treated
as a sequence of paragraphs. You can choose the output format (DOCX/TXT/VTT)
via \code{out_format} similar to \code{\link[=ghost_vtt]{ghost_vtt()}} and \code{\link[=ghost_batch]{ghost_batch()}}.
}
\examples{
# Writes report_redacted.docx in same folder, returns path:
# ghost_docx("report.docx", interviewers = "Dr. Smith", interviewees = "Jane Doe")
# Write as TXT instead of DOCX:
# ghost_docx("report.docx", interviewers = "Dr. Smith", interviewees = "Jane Doe",
#   out_format = "txt")
# With common names and redaction report:
# ghost_docx("report.docx", interviewers = "Dr. Smith", interviewees = "Jane Doe",
#   include_common_names = TRUE, report_redacted = TRUE)
}
